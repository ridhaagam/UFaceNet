model:
  image_size: 224
  backbone: tiny
  enable_frec: true
  enable_geometry: true
  enable_refiner: false
  frec_input_skip_init: 0.85
inference:
  tasks: all
  checkpoint: null
outputs:
  root: runs/ufacenet_base
